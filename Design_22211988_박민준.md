# 1. Design

<div align="center">
  <img width="800" height="400" alt="image" src="https://github.com/user-attachments/assets/cb3922d7-0031-47a2-b02d-bba4b990673a" />

</div>

<br>

# [RE:VIEW] 다중 플랫폼 리뷰 이상 탐지 대시보드
> **소속 및 인적사항**
> * **Student No:** 22211988
> * **Name:** 박민준
> * **E-mail:** akio7689@naver.com

---

##  Revision History

| Revision Date | Version | Description | Author |
| :---: | :---: | :--- | :---: |
| 26.05.24 | 1.0.0 | Design | Minjun Park |

---

##  Contents
1. Introduction
2. Class diagram
3. Sequence diagram
4. State machine diagram
5. Implementation requirements
6. Glossary
7. References

---

## 1. Introduction

본 Design 문서는 앞선 Conceptualization 및 Analysis 단계에서 정의된 'RE:VIEW' 다중 플랫폼 리뷰 이상 탐지 대시보드의 요구사항을 실제 구현 가능한 소프트웨어 설계로 구체화하기 위해 작성되었습니다. Analysis 단계에서 도출된 세부 기능(Use Case)들을 **9개의 핵심 시나리오로 세분화**하고, 이를 처리할 10개의 도메인 클래스를 기반으로 본 문서는 시스템이 '어떻게(How)' 동작해야 하는지에 대한 정적·동적 구조를 명세합니다.

'RE:VIEW' 시스템은 크게 5개의 계층으로 설계됩니다. 
* **Presentation Layer (React 웹 대시보드):** 사용자 인터페이스를 담당하며 직관적인 시각화 컴포넌트를 제공합니다.
* **API Layer (Java Spring Boot 기반 REST API 서버):** 클라이언트 요청을 수신하여 유효성을 검증하고 각 서비스 모듈에 로직을 위임합니다. 
* **Service Layer (ReviewService, AnalysisService, ReportService):** 핵심 비즈니스 로직인 데이터 정제, 어뷰징 이상 탐지 알고리즘 연산, PDF 리포트 생성을 독립적으로 처리합니다. (단, 무거운 웹 데이터 수집 및 NLP 연산은 Python 기반의 외부 AI 마이크로서비스인 PythonAIServer로 분리하여 위임합니다.)
* **Data Access Layer (Spring Data JPA):** 비즈니스 로직과 데이터베이스 사이의 추상화 계층을 제공하며, 영속성 컨텍스트를 활용해 SQL 작성을 자동화합니다.
* **Database (H2 In-Memory Database):** 수집된 리뷰 데이터, 감성 분석 결과, 플랫폼 통계 지표 및 이상 징후 로그를 영속적으로 저장합니다.

본 문서에서는 SRUP(Simplified Rational Unified Process) 방법론에 따라 다음을 순차적으로 기술합니다. 
Class Diagram에서는 시스템 내 핵심 클래스들의 속성, 메서드, 상호 관계를 정적으로 정의합니다. 
Sequence Diagram에서는 **정의된 9개의 핵심 Use Case**를 중심으로 실제 시스템 구현 코드의 메서드 호출과 일치하는 객체 간 동적 메시지 교환 흐름을 명세합니다.
State Machine Diagram에서는 UML 2.0 표준 규격에 따라 클라이언트(React Dashboard)와 메인 서버(Spring Boot)의 제어 상태 전이를 모델링합니다. 
마지막으로 Implementation Requirements에서는 시스템 구동 및 안정적 운영을 위한 구체적인 소프트웨어 기술 스택과 명확한 운영 환경 사양을 정의합니다.

---

## 2. Class diagram

### 2.1 Class diagram

<img width="1380" height="763" alt="image" src="https://github.com/user-attachments/assets/03776223-3048-4c24-9699-99d8d892aff8" />

# 2. Class Diagram Description 

본 장에서는 'RE:VIEW' 시스템의 실제 구현 아키텍처인 **Java Spring Boot 기반 메인 서버**와 **Python 기반 외부 AI 서버(External API)**의 결합 구조에 맞춰 구체화된 핵심 클래스들의 세부 속성(Attributes), 동작(Operations), 그리고 정적 관계를 명세합니다. 

각 클래스는 객체지향 설계의 핵심 원칙인 단일 책임 원칙(SRP)을 준수하도록 Controller, Service, Entity Layer로 철저히 분리되었으며, 무거운 AI 연산은 외부에 위임하여 10개의 핵심 클래스로 압축 설계되었습니다.

---

#### 2.2.1 Controller Layer

사용자(프론트엔드 대시보드)로부터 들어오는 외부 HTTP 요청을 안전하게 라우팅하고, 하위 `Service Layer`의 알맞은 비즈니스 로직으로 위임(Delegate)하는 진입점 계층입니다.

##### MainController (<<@RestController>>)
* **설명**: 웹 대시보드와 백엔드 시스템을 연결하는 유일한 컨트롤러 창구입니다.
* **클래스 세부 구조 명세**:

| 분류 | 항목명 | 데이터 타입 / 반환 타입 | 설명 |
| :--- | :--- | :--- | :--- |
| **Attributes** | `- reviewService` | `ReviewService` | 원시 리뷰 데이터 수집 및 DB 저장을 담당하는 서비스 의존성 |
| | `- analysisService` | `AnalysisService` | 어뷰징 탐지 및 통계 지표 산출을 담당하는 서비스 의존성 |
| | `- reportService` | `ReportService` | 최종 비교 리포트 생성 및 출력을 담당하는 서비스 의존성 |
| **Operations**| `+ search(place: String)` | `Map` | 단일 장소에 대한 데이터 수집 및 기초 분석 요청을 위임합니다. |
| | `+ getAnalysis(placeId: Long)` | `Map` | 특정 장소의 시계열 스파이크 및 이상 징후 리포트를 반환합니다. |
| | `+ comparePlaces(a: String, b: String)` | `Map` | 두 장소의 신뢰도 지표 상호 비교를 위한 병렬 데이터셋을 생성합니다. |

---

#### 2.2.2 Service Layer

시스템의 핵심 비즈니스 로직을 전담하는 '일꾼' 계층입니다. 기능의 성격에 따라 3개의 핵심 서비스 클래스로 격리하여 모듈의 응집도를 높였습니다.

##### ReviewService (<<@Service>>)
* **설명**: 프론트엔드의 검색 요청을 받아 파이썬 AI 서버로 크롤링을 지시하고, 받아온 원시 데이터를 JPA Entity를 통해 DB에 1차 적재합니다.
* **클래스 세부 구조 명세**:

| 분류 | 항목명 | 데이터 타입 / 반환 타입 | 설명 |
| :--- | :--- | :--- | :--- |
| **Attributes** | `- PYTHON_URL` | `String` | 외부 파이썬 크롤러/NLP 서버의 통신 엔드포인트 주소 |
| **Operations**| `+ getReviewData(place: String)` | `Map` | 파이썬 서버에 API를 호출하여 리뷰 및 감성 분석 결과를 받아옵니다. |
| | `+ savePlace(data: Map)` | `Place` | 수집된 장소 메타데이터를 DB에 저장합니다. |
| | `+ saveReviews(data: Map, place: Place)`| `void` | 분석된 리뷰 목록을 데이터베이스에 일괄 적재합니다. |
| | `+ saveDashboardReport(place: Place, score: double)`| `void` | 초기 대시보드 리포트 정보를 DB에 갱신합니다. |
| | `+ getReviewsByPlatform(placeId: Long, platform: String)`| `List` | 특정 플랫폼의 리뷰 데이터만을 필터링하여 반환합니다. |

##### AnalysisService (<<@Service>>)
* **설명**: 수집된 통계 지표와 가공 데이터를 연산하여 시계열 변칙성, 베이지안 평점 보정, 키워드 교차 검증을 수행합니다.
* **클래스 세부 구조 명세**:

| 분류 | 항목명 | 데이터 타입 / 반환 타입 | 설명 |
| :--- | :--- | :--- | :--- |
| **Attributes** | `+ ANOMALY_THRESHOLD` | `double = 15.0` | 변칙 탐지를 위한 기준 임계치 |
| | `+ SPIKE_WINDOW_DAYS` | `int = 7` | 시계열 스파이크 탐지를 위한 기준 일수 |
| | `+ GLOBAL_PRIOR_MEAN` | `double = 3.5` | 베이지안 평균 산출을 위한 시장 기본 평균값 |
| | `+ MIN_VOTE_COUNT` | `int = 10` | 베이지안 보정이 필요한 최소 리뷰 개수 기준 |
| **Operations**| `+ detectReviewSpike(placeId: Long)` | `boolean` | 단기간 리뷰 폭증 여부를 탐지합니다. |
| | `+ analyzeReviewerCredibility(placeId: Long)`| `Map` | 리뷰 작성자의 신뢰도 패턴을 분석합니다. |
| | `+ calculateAnomalyScore(placeId: Long)` | `double` | 종합적인 어뷰징 의심 점수를 도출합니다. |
| | `+ saveAnomalyLog(placeId: Long, type: String, value: double)` | `void` | 탐지된 이상 징후를 DB에 기록합니다. |
| | `+ calculateBayesianAverage(placeId: Long, platform: String)`| `double` | 표본 수가 적은 식당의 평점 신뢰도를 보정합니다. |
| | `+ applyRecencyWeight(placeId: Long)` | `double` | 최신 리뷰에 더 높은 가중치를 부여하는 로직을 수행합니다. |
| | `+ extractCrossKeywords(placeId: Long)`| `List` | 플랫폼 간 공통으로 언급된 핵심 키워드를 추출합니다. |
| | `+ savePlatformMetrics(placeId: Long)` | `void` | 계산 완료된 플랫폼별 지표를 DB에 갱신합니다. |

##### ReportService (<<@Service>>)
* **설명**: 최종 대시보드 맞춤형 비교 화면을 렌더링하고, PDF 레포트 출력을 위한 변환 작업을 담당합니다.
* **클래스 세부 구조 명세**:

| 분류 | 항목명 | 데이터 타입 / 반환 타입 | 설명 |
| :--- | :--- | :--- | :--- |
| **Operations**| `+ comparePlaces(nameA: String, nameB: String)`| `Map` | 두 장소 간의 기본 비교 데이터셋을 생성합니다. |
| | `+ buildParallelView(placeA: Place, placeB: Place)`| `Map` | 장소 A와 B의 지표를 1:1 병렬 구조로 매핑합니다. |
| | `+ rankByBayesianScore(placeA: Long, placeB: Long)`| `String` | 보정된 베이지안 점수를 기반으로 우위 장소를 판별합니다. |
| | `+ buildFinalHTMLTemplate(data: Map)` | `String` | 종합 데이터를 HTML 템플릿 코드 구조로 렌더링합니다. |
| | `+ exportToPDF(placeId: Long)` | `byte` | 렌더링된 리포트를 PDF 바이너리 스트림으로 변환합니다. |

---

#### 2.2.3 External API

자바 스프링 부트 메인 서버의 부하를 줄이기 위해 별도로 분리된 파이썬 기반의 마이크로서비스입니다.

##### PythonAIServer (<<External API>>)
* **설명**: 자바 서버의 HTTP 요청을 받아, Selenium 기반의 웹 크롤링과 기학습된 모델(KoELECTRA 등) 기반의 감성 분석(NLP)을 독립적으로 수행한 뒤 결과를 반환합니다.

---

#### 2.2.4 Entity Layer (JPA)

물리 데이터베이스 테이블과 매핑되는 도메인 모델 클래스들입니다. 정보 은닉 및 캡슐화 원칙에 따라 자체적인 유효성 검증 및 상태 관리 비즈니스 동작을 내포합니다.

##### Place (<<@Entity>>)
* **설명**: 추적 및 분석 대상인 식당/장소의 마스터 메타데이터 엔티티입니다.

| 분류 | 항목명 | 데이터 타입 | 설명 |
| :--- | :--- | :--- | :--- |
| **Attributes** | `+ id` | `Long` | 고유 식별 번호 (PK) |
| | `+ placeName` | `String` | 대상 장소/식당명 |
| | `+ naverPlaceUrl` | `String` | 네이버 플레이스 타겟 주소 |
| | `+ kakaoPlaceUrl` | `String` | 카카오 맵 타겟 주소 |
| | `+ createdAt` | `LocalDateTime` | 레코드 최초 생성 일시 |
| **Operations**| `+ getActivePlatforms()` | `List<String>`| 유효한 URL 주소가 세팅된 활성 크롤링 플랫폼을 판별합니다. |

##### Review (<<@Entity>>)
* **설명**: 수집된 개별 리뷰의 텍스트 본문과 메타 정보를 보관하는 엔티티입니다.

| 분류 | 항목명 | 데이터 타입 | 설명 |
| :--- | :--- | :--- | :--- |
| **Attributes** | `+ id` | `Long` | 리뷰 고유 식별 번호 (PK) |
| | `+ place` | `Place` | 매핑된 대상 장소 객체 (FK) |
| | `+ platform` | `String` | 수집 출처 플랫폼 구분 |
| | `+ contentText` | `String` | 리뷰 텍스트 본문 |
| | `+ starRating` | `Double` | 사용자 부여 평점 |
| | `+ writtenDate` | `String` | 플랫폼 상의 원시 작성일 |
| | `+ collectedAt` | `LocalDateTime` | 시스템 수집 일시 |
| **Operations**| `+ isPositive()` | `Boolean` | 감성 분석 결과를 토대로 긍정 리뷰 여부를 자가 판별합니다. |

##### AnomalyLog (<<@Entity>>)
* **설명**: 어뷰징 탐지 엔진에 의해 걸러진 변칙(Spike, 괴리율 등) 이력 데이터 로그 엔티티입니다.

| 분류 | 항목명 | 데이터 타입 | 설명 |
| :--- | :--- | :--- | :--- |
| **Attributes** | `+ id` | `Long` | 로그 고유 레코드 번호 (PK) |
| | `+ place` | `Place` | 매핑된 대상 장소 객체 (FK) |
| | `+ anomalyType` | `String` | 탐지된 변칙의 종류 |
| | `+ threshold` | `Double` | 탐지 기준이 된 임계치 |
| | `+ detectedValue` | `Double` | 탐지 시점의 초과 수치 |
| | `+ detectAt` | `LocalDateTime` | 이상 징후 포착 일시 |
| **Operations**| `+ recordDetection(type: String, value: Double)`| `void` | 스스로의 이상 징후 상태를 기록하고 갱신합니다. |

##### PlatformMetrics (<<@Entity>>)
* **설명**: 특정 장소의 플랫폼별 통계 지표와 베이지안 보정값을 누적하는 엔티티입니다.

| 분류 | 항목명 | 데이터 타입 | 설명 |
| :--- | :--- | :--- | :--- |
| **Attributes** | `+ id` | `Long` | 통계 지표 고유 번호 (PK) |
| | `+ place` | `Place` | 매핑된 대상 장소 객체 (FK) |
| | `+ platform` | `String` | 해당 통계의 소속 플랫폼 |
| | `+ totalReviewCount` | `Integer` | 누적 총 리뷰 수 |
| | `+ bayesianAvgScore` | `Double` | 최종 보정된 베이지안 평균 점수 |
| | `+ recencyWeightScore` | `Double` | 최신성이 반영된 가중치 점수 |
| | `+ collectAt` | `LocalDateTime` | 통계 산출 기준 일시 |
| **Operations**| `+ updateBayesianScore(score: Double)` | `void` | 새롭게 산출된 평점 지표값으로 상태를 안전하게 갱신합니다. |

##### DashboardReport (<<@Entity>>)
* **설명**: 최종 사용자 화면인 대시보드에 다이렉트로 바인딩되는 분석 요약 데이터의 종착지 엔티티입니다.

| 분류 | 항목명 | 데이터 타입 | 설명 |
| :--- | :--- | :--- | :--- |
| **Attributes** | `+ id` | `Long` | 요약 리포트 고유 번호 (PK) |
| | `+ place` | `Place` | 매핑된 대상 장소 객체 (FK) |
| | `+ discrepancyRate` | `Double` | 플랫폼 간의 별점 괴리율 최종 수치 (%) |
| | `+ crossKeywords` | `String` | 공통 교차 검증된 핵심 키워드 목록 |
| | `+ isAbusingSuspected` | `Boolean` | 최종 복합 어뷰징 조작 의심 판정 플래그 |
| | `+ generateAt` | `LocalDateTime` | 리포트 최종 생성 일시 |
| **Operations**| `+ updateDiscrepancyRate()` | `void` | 수집된 양 플랫폼의 평점을 기반으로 자체 괴리율을 산출합니다. |

---

## 3. Sequence diagram  

본 장에서는 Conceptualization 단계에서 정의된 9개의 핵심 Use Case를 기반으로, 클라이언트(React 웹 대시보드)와 메인 백엔드 서버(Java Spring Boot), 
그리고 외부 AI 연산 모듈(Python) 간의 동적 메시지 교환 흐름을 명세합니다. 각 시나리오는 RESTful API 통신 제어 흐름부터 JPA ORM을 통한 
데이터베이스 영속화(Persistence) 과정까지의 세부 트랜잭션을 포함합니다.

---

## 3.1. 장소 검색

사용자가 대시보드 화면에서 특정 식당이나 장소명을 검색했을 때, 백엔드가 무조건 새로운 크롤링을 수행하는 대신 데이터 베이스에 기존 장소 정보가 존재하는지 선행 룩업(Lookup)하여 유효성을 검증하는 흐름입니다.

<img width="909" height="632" alt="image" src="https://github.com/user-attachments/assets/94c47743-9218-4ef3-b083-a0b02320d0d8" />

| 구분 (처리 단계) | 메시지 순서 | 객체 간 상호작용 및 상세 비즈니스 로직 |
| :--- | :---: | :--- |
| **HTTP 요청 수신 및 위임** | `1 ~ 2` | 사용자가 장소명을 검색하면 `search(place)` API가 호출되며, `MainController`는 이를 `ReviewService.getReviewData(place)`로 위임합니다. |
| **기존 데이터 검증**| `3 ~ 4` | 중복 수집을 방지하기 위해 `PlaceRepository.findByPlaceName(name)`을 통해 DB를 선행 조회하고 결과를 리턴(`return`) 받습니다. |
| **파이프라인 가동 응답** | `5 ~ 6` | 신규 장소로 판정될 경우 프론트엔드에 `return(null)`을 전달하고, 최종적으로 `responseAccepted()` (HTTP 202) 신호를 응답하여 수집 파이프라인을 가동합니다. |

---

### 3.2. 리뷰 데이터 통합 조회

파이썬 서버가 수집 및 감성 분석을 마친 원시 JSON 데이터를 자바 백엔드가 엔티티 객체로 매핑하고, JPA 영속성 컨텍스트를 활용해 외래키(FK) 제약조건과 함께 DB에 일괄 저장하는 파이프라인 흐름입니다.

<img width="1154" height="466" alt="image" src="https://github.com/user-attachments/assets/6bb74de3-739d-4d0a-91cb-bd584a669fbc" />

| 구분 (처리 단계) | 메시지 순서 | 객체 간 상호작용 및 상세 비즈니스 로직 |
| :--- | :---: | :--- |
| **이종 시스템 통신** | `1 ~ 2` | `ReviewService`가 `PythonAIServer`로 `GET /api/crawl...` 통신을 요청하여 분석 완료된 데이터를 `return(rawData)` 형태로 수신합니다. |
| **마스터 엔티티 영속화**| `3 ~ 4` | 장소 메타정보를 `save(newPlace)`를 통해 마스터 테이블에 저장하고, 생성된 엔티티를 `return(savedPlace)`로 반환받습니다. |
| **연관 데이터 일괄 적재**| `5 ~ 8` | 생성된 기본키를 외래키로 참조하여 `ReviewRepository.saveAll(reviews)` 및 `DashboardReportRepository.save(report)`를 순차 호출해 RDBMS 스택에 완전 적재합니다. |

---

### 3.3. 작성자 신뢰도 분석

수집된 리뷰를 바탕으로 작성자의 동일 텍스트 도배성 패턴이나 평점 극단값 남발 여부 등을 내부 가중치 알고리즘으로 스코어링하는 심층 필터링 단계입니다.

<img width="831" height="419" alt="image" src="https://github.com/user-attachments/assets/3ec99c66-e63e-4b6d-98eb-c673a5ae5af0" />

| 구분 (처리 단계) | 메시지 순서 | 객체 간 상호작용 및 상세 비즈니스 로직 |
| :--- | :---: | :--- |
| **도메인 데이터 로드** | `1 ~ 3` | `analyzeReviewerCredibility(placeId)` 호출 시, `ReviewRepository.findByPlaceId(placeId)`를 통해 해당 장소의 리뷰 묶음을 전량 인출하여 `return(reviewList)` 받습니다. |
| **패턴 연산 및 필터링** | `4 ~ 5` | `AnalysisService` 내부에서 `calculateAnomalyScore()` 로직을 가동하여 페널티를 부여하고, 최종 신뢰도 지표를 `return(credibilityMap)`으로 컨트롤러에 반환합니다. |

---

### 3.4. 분석 조건 설정

사용자가 "네이버 플랫폼 리뷰만 보기" 등 특정 필터를 적용했을 때, 데이터베이스 레벨에서 인덱스를 활용하여 특정 데이터만 선별적으로 인출하는 동적 조회 흐름입니다. 

<img width="888" height="381" alt="image" src="https://github.com/user-attachments/assets/cff314cc-b045-4064-b91b-9fdc880a5f99" />

| 구분 (처리 단계) | 메시지 순서 | 객체 간 상호작용 및 상세 비즈니스 로직 |
| :--- | :---: | :--- |
| **조건부 요청 라우팅** | `1 ~ 2` | 사용자가 플랫폼 필터를 조작하면 `getReviewsByPlatform()` 메서드를 통해 쿼리 파라미터를 파싱하고 로직을 바인딩합니다. |
| **조건절 쿼리 수행** | `3 ~ 4` | 전체 스캔을 방지하기 위해 `ReviewRepository.findByPlaceIdAndPlatform()` 명명 규칙 쿼리를 수행하여 타겟 엔티티 리스트를 `return(filteredReviews)` 받습니다. |
| **결과 응답** | `5 ~ 6` | 조회된 데이터를 컨트롤러를 거쳐 `return(jsonResponse)` 형태로 클라이언트에 최종 전달합니다. |

---

### 3.5. 감성 괴리율 조회

객체지향의 캡슐화 원칙을 적용하여, 통합 레포트 엔티티 객체가 스스로 내부 상태 데이터를 대조 연산해 양대 플랫폼 간의 감성 평점 격차 백분율을 도출하는 도메인 로직 흐름입니다. 

<img width="994" height="390" alt="image" src="https://github.com/user-attachments/assets/c172d890-f8c8-435c-9d3b-60757f378ccf" />

| 구분 (처리 단계) | 메시지 순서 | 객체 간 상호작용 및 상세 비즈니스 로직 |
| :--- | :---: | :--- |
| **요약 레포트 인출** | `1 ~ 2` | `DashboardReportRepository.findByPlaceId()`를 호출하여 대상 장소의 요약 레포트 객체를 `return(reportEntity)`로 로드합니다. |
| **도메인 객체 자가 연산**| `3 ~ 4` | 인출된 엔티티의 내부 비즈니스 메서드인 `updateDiscrepancyRate()`를 직접 호출하여 객체 스스로 연산한 격차 수치를 `return(discrepancyRate)`로 반환받습니다. |

---

### 3.6. 어뷰징 알림 확인

시계열 데이터 분석을 통해 특정 날짜에 평점이나 리뷰 수가 비정상적으로 폭증(Spike)한 정황을 탐지하고, 이를 위험 로그('AnomalyLog')로 영속화하여 대시보드 알림을 발생시키는 흐름입니다. 

<img width="1207" height="476" alt="image" src="https://github.com/user-attachments/assets/3514a67b-9f66-4d1e-a069-a4f2809c819c" />

| 구분 (처리 단계) | 메시지 순서 | 객체 간 상호작용 및 상세 비즈니스 로직 |
| :--- | :---: | :--- |
| **시계열 데이터 인출** | `1 ~ 2` | 빈도수 통계 그룹화를 위해 `ReviewRepository.findByPlaceId()`를 호출하고 `return(reviewList)`로 데이터를 확보합니다. |
| **알림 생성 및 영속화** | `3 ~ 5` | 내부의 `detectReviewSpike()` 알고리즘이 임계치 초과를 감지하면, 즉시 `AnomalyLogRepository.save(anomalyLog)`를 통해 알림 DB에 기록합니다. |
| **알림 리스트 표출** | `6 ~ 9` | 사용자가 경고창을 열면 `getAnalysis(placeId)`가 호출되며, 로그 테이블을 재조회하여 실시간 알림 목록을 `return(jsonAlerts)` 형태로 렌더링합니다. |

---

### 3.7. 결과 교차 키워드 조회

서비스 계층에서 양대 플랫폼 리뷰 텍스트 내의 형태소(명사) 배열을 상호 대조하여, 공통으로 빈도수가 급증한 바이럴 조작 의심 키워드 교집합을 도출하는 흐름입니다. 

<img width="768" height="541" alt="image" src="https://github.com/user-attachments/assets/e4c709b2-1ad9-478d-8578-f53c9ce6a45c" />

| 구분 (처리 단계) | 메시지 순서 | 객체 간 상호작용 및 상세 비즈니스 로직 |
| :--- | :---: | :--- |
| **플랫폼별 키워드 로드** | `1 ~ 5` | `extractCrossKeywords()`가 기동되며, 네이버와 카카오의 텍스트 컬렉션을 `findTextByPlaceIdAndPlatform()`으로 각각 인출해 옵니다. |
| **명사 교집합 연산** | `6 ~ 7` | 내부 자가 호출을 통해 양쪽 플랫폼에서 공통으로 노출된 핵심 명사 리스트를 정제하고 `return(crossKeywords)`로 반환합니다. |
| **결과 응답** | `8` | 최종 추출된 교차 키워드 리스트를 `bindToJson()`을 통해 래핑하여 프론트엔드에 전달합니다. |

---

### 3.8. 분석 리포트 공유

분산된 지표 데이터를 단일 스냅샷으로 결합하고, 백엔드 내부의 Document 변환 엔진을 통해 PDF로 렌더링하여 다운로드를 제공하는 흐름입니다.

<img width="1299" height="554" alt="image" src="https://github.com/user-attachments/assets/50ac2b44-6d23-498e-82f3-b067946901c2" />

| 구분 (처리 단계) | 메시지 순서 | 객체 간 상호작용 및 상세 비즈니스 로직 |
| :--- | :---: | :--- |
| **종합 통계 원격 인출** | `1 ~ 6` | `exportToPDF()`가 호출되면 `PlaceRepository.findById()`와 `DashboardReportRepository`를 통해 가공 완료된 통계 엔티티들을 순차적으로 모두 확보합니다. |
| **스트림 렌더링/변환** | `7 ~ 8` | 확보한 지표를 `buildFinalHTMLTemplate()`에 바인딩하고, 변환 모듈을 거쳐 물리적인 바이트 배열 형태의 `return(pdfStream)`으로 압축 반환합니다. |
| **HTTP 파일 응답** | `9` | 컨트롤러에서 `downloadPdf()` 동작을 통해 Content-Type이 명시된 응답을 클라이언트 브라우저에 반환하여 파일 다운로드를 트리거합니다. |

---

## 3.9. 타겟 장소 비교 분석

사용자가 지정한 두 장소의 통계 지표 엔티티를 동시 룩업한 뒤, 병렬 어레이 구조로 조합하여 대시보드 화면상에서 대칭 시각화를 지원하는 처리 흐름입니다.

<img width="1090" height="560" alt="image" src="https://github.com/user-attachments/assets/90998653-2f58-4030-833f-c94cc80b4e93" />

| 구분 (처리 단계) | 메시지 순서 | 객체 간 상호작용 및 상세 비즈니스 로직 |
| :--- | :---: | :--- |
| **멀티 도메인 룩업** | `1 ~ 6` | `comparePlaces(nameA, nameB)`가 호출되며, `PlatformMetricsRepository.findByPlaceId()`를 통해 장소 A와 장소 B의 보정 지표 엔티티를 순서대로 `return(metrics)` 받습니다. |
| **컴포지션 맵 빌드** | `7 ~ 8` | 내부의 `buildParallelView()` 메서드가 두 엔티티의 베이지안 평균, 괴리율 데이터를 1:1 대칭 맵 구조로 병합 처리하여 `return(comparisonMap)`을 반환합니다. |
| **비교 레이아웃 표출** | `9` | 구조화가 완료된 병렬 비교 벡터 데이터가 `return(jsonResponse)`로 전달되어, 좌우 대칭 차트로 화면에 렌더링됩니다. |
---

## 4. State machine diagram

<img width="872" height="722" alt="image" src="https://github.com/user-attachments/assets/aeb5a8c3-fb32-49e8-ad81-4532988ecf2a" />

### 4.1. Client System State Description (웹 대시보드)

| 상태명 (State) | 진입/수행 동작 (Entry/Do Action) | 상태 전이 조건 (Trigger / Guard) | 다음 상태 (Next State) | 상태 상세 설명 (Description) |
| :--- | :--- | :--- | :--- | :--- |
| **Initialize** | `Entry` 정적 에셋 로드<br>`Do` 렌더링 트리 구축 | 로드 완료 | `Idle` | 브라우저가 React 앱을 최초로 로드하고 DOM 트리를 구축하는 초기화 단계입니다. |
| **Idle** | `Do` 사용자 검색어 입력 대기 | 검색 버튼 클릭 | `InputValidation` | 앱 로딩이 완료되어 사용자의 액션(검색)을 대기하는 기본 유휴 상태입니다. |
| **InputValidation**| `Do` 검색어 정규식 검사 및 특수문자 필터링 | [유효한 검색어] API 호출<br>[유효하지 않은 검색어] | `AwaitingResponse`<br>`Idle` | 사용자가 입력한 검색어의 유효성을 클라이언트 단에서 선행 검증하는 상태입니다. |
| **AwaitingResponse**| `Entry` 스피너 UI 활성화<br>`Do` API Polling 대기 | [HTTP 200 OK] JSON 수신<br>[타임아웃 or HTTP 500] | `DataParsing`<br>`ErrorState` | 백엔드로 데이터를 요청한 후, 비동기 응답이 올 때까지 로딩 UI를 띄우고 대기하는 상태입니다. |
| **DataParsing** | `Do` JSON 데이터 모델 매핑 및 차트 데이터 가공 | [파싱 성공] 렌더링 시작<br>[데이터 Null] Empty UI 표출 | `DashboardRendered`<br>`EmptyState` | 백엔드로부터 전달받은 JSON 데이터를 프론트엔드 차트 컴포넌트에 맞게 객체로 매핑하는 상태입니다. |
| **DashboardRendered**| `Do` 베이지안 평점 차트 및 워드클라우드 렌더링 | 필터 클릭 [플랫폼 선택]<br>비교 장소 추가 [Target B]<br>내보내기 클릭 | `Filtering`<br>`CompareMode`<br>`PDFExporting` | 성공적으로 파싱된 데이터를 기반으로 종합 대시보드 화면을 렌더링하여 사용자에게 표출하는 메인 상태입니다. |
| **Filtering** | - | 렌더링 업데이트 | `DashboardRendered` | 특정 플랫폼 조건에 맞춰 렌더링 트리를 갱신하는 일시적 상태입니다. |
| **CompareMode** | `Do` 두 장소 병렬 차트 렌더링 | 비교 해제 / 단일 뷰 복귀 | `DashboardRendered` | 두 개의 대상 장소 지표를 1:1 대칭으로 렌더링하여 비교 분석 화면을 제공하는 상태입니다. |
| **PDFExporting** | `Do` HTML to Canvas 변환 및 Blob 스트림 생성 | [변환 완료] 다운로드 트리거 | `DashboardRendered` | 렌더링된 화면 데이터를 브라우저 단에서 PDF 바이너리(Blob)로 변환하여 다운로드하는 상태입니다. |
| **ErrorState** / **EmptyState** | - | 재검색 요청 / 메인 복귀 | `Idle` | 통신 에러 발생 또는 분석 결과가 없을 경우 예외 화면을 표출하는 상태입니다. |

---

### 4.2. Server System State Description (Spring Boot 메인 서버)

| 상태명 (State) | 진입/수행 동작 (Entry/Do Action) | 상태 전이 조건 (Trigger / Guard) | 다음 상태 (Next State) | 상태 상세 설명 (Description) |
| :--- | :--- | :--- | :--- | :--- |
| **Booting** | `Do` Spring Context 초기화 및 H2 DB 커넥션 풀 생성 | 앱 구동 완료 | `Listening` | Spring Boot 애플리케이션이 실행되며 의존성(DI) 주입 및 데이터베이스 커넥션을 맺는 준비 상태입니다. |
| **Listening** | `Do` Tomcat 스레드 풀 가동 | GET `/api/search` 요청 수신 | `RequestValidation` | 내장 웹 서버(Tomcat)가 가동되어 프론트엔드의 REST API 요청을 수신 대기하는 기본 상태입니다. |
| **RequestValidation**| `Do` 파라미터 유효성 검증 | [검증 통과] 캐시 조회 | `DB_Lookup` | 컨트롤러 단에서 HTTP 요청 파라미터의 유효성 및 악의적 접근을 방어하는 상태입니다. |
| **DB_Lookup** | `Do` `PlaceRepo.findByPlaceName()` | [미존재 or 갱신 필요]<br>[데이터 존재 & 최신 상태] | `ExternalDelegation`<br>`ResponseReady` | 반복적인 크롤링 부하를 막기 위해 JPA를 통해 DB에 기존 수집된 데이터가 있는지 먼저 조회하는 상태입니다. |
| **ExternalDelegation**| `Entry` 외부 API 타임아웃 세팅<br>`Do` Python 크롤링/NLP 요청 | [JSON 수신 완료]<br>[파이썬 서버 응답 없음] | `NLP_Processing`<br>`ErrorHandling` | Python 마이크로서비스로 Selenium 기반 데이터 수집 및 감성 분석을 위임(Delegate)하는 상태입니다. |
| **NLP_Processing** | `Do` 스파이크 탐지(StdDev), 베이지안 보정, 교차 명사 추출 | [연산 성공] Entity 매핑<br>[연산 중 예외 발생] | `DB_Persisting`<br>`ErrorHandling` | 수집된 원시 데이터를 바탕으로 어뷰징 탐지 및 지표 보정을 위한 도메인 비즈니스 로직을 수행하는 상태입니다. |
| **DB_Persisting** | `Entry` `@Transactional` 시작<br>`Do` `saveAll()` 호출<br>`Exit` 트랜잭션 Commit | [Commit 성공]<br>[DB 제약조건 위배] | `ResponseReady`<br>`ErrorHandling` | 가공 완료된 도메인 객체들을 영속성 컨텍스트를 거쳐 H2 DB에 물리적으로 저장하는 트랜잭션 상태입니다. |
| **ResponseReady** | `Do` DTO 조립 및 JSON 직렬화 | [응답 전송] HTTP 200 OK | `Listening` | DB 조회 또는 신규 분석이 완료된 데이터를 클라이언트 응답용 DTO 포맷으로 변환하는 상태입니다. |
| **ErrorHandling** | `Do` Transaction Rollback 및 `GlobalExceptionHandler` 가동 | [예외 처리] 에러 로깅 및 HTTP 500 응답 | `Listening` | 처리 도중 발생한 모든 예외 상황에 대해 트랜잭션을 롤백하고 서버 다운을 방지하는 예외 제어 상태입니다. |

---

## 5. Implementation requirements
본 장에서는 'RE:VIEW' 시스템을 실제로 구현하고 구동하기 위해 필요한 소프트웨어 기술 스택 및 운영 환경(Operating Environments)을 Client, Main Server, External AI Server, Database 계층으로 분류하여 명세합니다.

### 5.1 Development & Operating Environments

 계층 (Layer) | 구분 | 요구 사양 및 기술 스택 | 설명 (사용 목적 및 기대 효과) |
| :--- | :--- | :--- | :--- |
| **Client** | Web Framework | React.js (HTML5, CSS3, JavaScript) | SPA(Single Page Application) 기반의 동적 대시보드 UI 렌더링 및 실시간 차트 시각화 구현 |
| | Runtime Env. | Node.js (v18.x 이상) | 프론트엔드 패키지 매니징(npm) 및 비동기 개발 환경 구축 |
| | Browser | Chrome, Edge, Safari | 웹 표준(Web Standards)을 지원하는 모던 브라우저 환경에서 대시보드 접근 및 렌더링 |
| **Main Server** | Language | Java 17 | 시스템의 높은 안정성과 객체지향적 도메인 설계를 위한 메인 프로그래밍 언어 |
| | Framework | Spring Boot (3.x) | RESTful API 라우팅, 의존성 주입(DI), 내장 Tomcat을 활용한 무상태(Stateless) 백엔드 인프라 구축 |
| | Build Tool | Gradle | 라이브러리 의존성 관리 및 시스템 빌드 자동화 |
| | IDE | IntelliJ IDEA | 자바 기반 백엔드 시스템 코드 작성, 트랜잭션 관리 및 디버깅 환경 |
| **External API** | Language | Python 3.10 이상 | 무거운 AI 연산 및 동적 데이터 수집 모듈 구축을 위한 언어 |
| | Web / Crawler | FastAPI / Selenium | 자바 서버와의 초경량 비동기 HTTP 통신 및 웹 브라우저 자동화를 통한 동적 SPA 페이지 웹 스크래핑 |
| | AI / NLP | KoELECTRA, PyTorch (Transformers) | RTD(Replaced Token Detection) 방식을 활용한 빠르고 가벼운 자연어 형태소 분석 및 긍/부정 감성 수치화 |
| | IDE | VS Code | 파이썬 스크립트 작성 및 독립 마이크로서비스 구동 환경 |
| **Database** | RDBMS | H2 Database (In-Memory) | 시스템 개발 및 검증 단계에서 영속성 컨텍스트(JPA) 연동을 위한 초경량 인메모리 관계형 데이터베이스 |
| | ORM | Spring Data JPA (Hibernate) | 자바 도메인 객체(Entity)와 물리 DB 테이블 간의 임피던스 불일치 해소 및 SQL 쿼리 자동화 |

---

## 6. Glossary

본 문서 및 시스템 내에서 사용되는 주요 전문 용어와 약어를 명확히 정의하여 프로젝트 이해관계자 간의 의사소통 혼선을 방지합니다.

* **교차 검증 (Cross-check):** 특정 장소에 대한 객관성을 확보하기 위해, 단일 플랫폼의 리뷰에 의존하지 않고 네이버 지도와 카카오맵 양쪽의 데이터를 상호 대조하는 검증 기법.
* **감성 괴리율 (Sentiment Discrepancy Rate):** AI가 분석한 양대 플랫폼의 실제 긍정/부정 여론(감성 지수)의 차이를 백분율(%)로 수치화한 값. 보상형 리뷰 이벤트 등으로 인한 특정 플랫폼의 평점 인플레이션을 직관적으로 보여주는 지표.
* **자연어 처리 (NLP, Natural Language Processing):** 인간의 언어(한국어 리뷰)를 컴퓨터가 읽고 그 문맥과 숨은 의도를 파악하여 처리하도록 돕는 인공지능 기술.
* **어뷰징 (Abusing):** 마케팅 업체나 봇(Bot)을 동원하여 영수증 리뷰를 허위로 작성하거나, 생성형 AI를 이용해 가짜 긍정 리뷰를 대량 생산하여 소비자 여론을 조작하는 부당 행위.
* **시계열 스파이크 (Time-series Spike):** 특정 장소의 일일/월간 리뷰 등록 빈도수가 평소 평균치를 강하게 초과하여 단기간에 비정상적으로 솟구치는(폭증하는) 패턴.
* **베이지안 평균 (Bayesian Average):** 리뷰가 극단적으로 적은 업체(예: 리뷰 3개, 평점 5.0)와 충분히 누적된 업체(예: 리뷰 1,000개, 평점 4.5)를 단순 비교할 때 발생하는 통계적 왜곡을 방지하기 위해, 전체 표본의 평균(사전 정보)을 가중치로 반영하여 신뢰할 수 있게 평점을 보정하는 기법.
* **SPA (Single Page Application):** 사용자가 웹 페이지를 이동할 때마다 화면 전체를 새로고침하지 않고, 필요한 데이터만 비동기적으로 불러와 화면의 일부분만 동적으로 렌더링하는 현대적인 웹 애플리케이션 구조. (네이버, 카카오 지도의 기본 구조)
* **Selenium (셀레니움):** 파이썬 코드를 통해 웹 브라우저(Chrome 등)를 실제로 띄우고 사람처럼 마우스 스크롤, 버튼 클릭 등을 자동화할 수 있는 테스팅 및 크롤링 프레임워크. SPA 구조의 리뷰를 수집하는 핵심 도구.
* **KoELECTRA:** 텍스트 중간에 구멍(Mask)을 뚫고 맞히는 기존 방식(BERT)과 달리, 가짜 단어를 섞어두고 판별자(Discriminator)가 진짜인지 가짜인지 찾아내는 RTD 방식을 사용하여 훨씬 가벼우면서도 한국어 문맥 분석 성능이 뛰어난 딥러닝 기반 언어 모델.

---

## 7. References

1. 카카오 로컬 API 공식 개발자 문서 가이드, "Kakao Developers", https://developers.kakao.com/docs/latest/ko/local/dev-guide
2. 네이버 클라우드 플랫폼 Maps API, "Naver Cloud Platform", https://www.ncloud.com/v2/product/applicationService/maps
3. 한국경제, "너무 맛있어요 리뷰 넘쳐나더니...소문난 맛집의 배신", https://www.hankyung.com/article/202409021724g, 2024.09.02.
4. 포인트데일리, "네이버, 5년 만에 별점 제도 부활...4월 6일부터 플레이스 리뷰 개편", https://www.pointdaily.co.kr/news/articleView.html?idxno=296790, 2026.03.18.
5. Hugging Face, "KoELECTRA: Pre-trained Language Model for Korean", https://huggingface.co/monologg/koelectra-base-v3-discriminator


